const fs=require('fs');let p='work/test-audio.cjs',s=fs.readFileSync(p,'utf8').replace('Object.keys(a.buffers).length,23','Object.keys(a.buffers).length,33').replace('PASS: 23 mappings','PASS: 33 mappings');s=s.replace("a.mute(true);","a.play('mc-voice');a.ctx.currentTime+=.2;a.pause();assert.equal(a.resumeClips.length,1);a.start();assert([...a.voices].some(s=>s.clip?.name==='mc-voice'&&s.offset>.19));a.stopCinematic();assert.equal(a.voices.size,0);a.mute(true);");fs.writeFileSync(p,s);
p='work/heist-audit.cjs';s=fs.readFileSync(p,'utf8').replace('opening.time>=10','opening.time>=16.2');fs.writeFileSync(p,s);
